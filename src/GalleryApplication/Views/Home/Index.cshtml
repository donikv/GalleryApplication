@{
    ViewData["Title"] = "Home Page";
}

@using System.Threading.Tasks
@using ApplicationClasses
@using ApplicationClasses.Models
@model ApplicationClasses.Models.User

@{
    Layout = "_Layout";
}

<!DOCTYPE html>

<html>
<head>
    <title>Home</title>
    <style>
        h2 {
            font-size: 12px;
        }
    </style>
</head>
<body>
@if (Model != null)
{
    <h1><a asp-controller="User" asp-action="Index" asp-route-id="@Model.Id">Hello @Model.Name</a></h1>
    <br/>
}
else
{
    <h1>Prazno je</h1>
}

<div>

    <br/>
    @*        <a asp-controller="Home" asp-action="SeeCompleted"> See completed Todos</a>
        @if (Model != null)
        {
            foreach (var todo in Model)
            {
                <h2>
                    @todo.Text <br /> @todo.DateCreated <br /> <a asp-controller="Todo" asp-action="MarkCompleted" asp-route-id="@todo.Id">Mark completed</a>
                </h2>
            }
        }*@


</div>
</body>
</html>
